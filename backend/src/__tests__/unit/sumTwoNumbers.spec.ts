describe('Sum two numbers', () => {
    it('should sum two numbers', () => {
        const firstNumber = 2;
        const secondNumber = 2;

        expect(firstNumber + secondNumber).toBe(4);
    });
});
